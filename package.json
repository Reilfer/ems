{
    "name": "reilfer-eduv",
    "version": "0.1.0",
    "private": true,
    "description": "ReilferEDUV - Hệ thống quản lý giáo dục toàn diện",
    "scripts": {
        "dev": "pnpm -r dev",
        "build": "pnpm -r build",
        "lint": "pnpm -r lint",
        "test": "pnpm -r test",
        "db:migrate": "cd database && ./migrate.sh",
        "db:seed": "ts-node database/seeds/seed.ts"
    },
    "engines": {
        "node": ">=20.0.0",
        "pnpm": ">=9.0.0"
    },
    "devDependencies": {
        "@prisma/client": "^5.22.0",
        "@types/bcryptjs": "^3.0.0",
        "prisma": "^5.22.0",
        "ts-node": "^10.9.2",
        "tsx": "^4.21.0"
    },
    "dependencies": {
        "@prisma/client": "^5.22.0",
        "bcryptjs": "^3.0.3"
    }
}
